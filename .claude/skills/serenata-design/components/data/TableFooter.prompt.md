Pagination footer that always accompanies `DataTable`.

```jsx
<TableFooter shown={10} total={128} perPage={10} onPerPageChange={setPerPage} />
```

Counts are plain muted text, never buttons; there are no numbered page links in the source UI.
