Frame for every authenticated screen.

```jsx
<AppShell sidebar={<Sidebar … />} topbar={<Topbar … />}>
  <SectionHero … />
  <Card>…</Card>
</AppShell>
```

Content padding is 36px and the column is fluid — never centre or cap the main content width.
