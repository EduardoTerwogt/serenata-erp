Opens every main view: a compact brand-texture band with the section title and its primary action.

```jsx
<SectionHero title="Cotizaciones" action={<Button>Nueva cotización</Button>} />
```

Title only — no eyebrow and no subtitle in the current design (both props still exist for other uses). Keep it short vertically so the table is visible without scrolling.
