The strip at the top of the content column — transparent, belongs to the content area, not the sidebar.

```jsx
<Topbar user={{ name: 'Carla Mendoza', nickname: '@carlam', initials: 'CM' }} />
```

There is no global search field and no theme control: the product is dark-only and search happens per section, above each table.
