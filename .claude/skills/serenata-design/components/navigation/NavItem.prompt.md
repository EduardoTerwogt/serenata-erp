A sidebar navigation row: optional icon, label, whole row clickable.

```jsx
<NavItem icon="file-text" label="Cotizaciones" active />
<NavItem icon="users" label="Clientes" />
```

Exactly one active item per view. Inactive rows are muted text on transparent; hover raises them to the row surface.
