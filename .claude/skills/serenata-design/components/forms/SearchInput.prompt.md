Search field on the dark input surface with a muted magnifier glyph.

```jsx
<SearchInput pill fullWidth placeholder="Buscar cotizaciones, clientes, proyectos…" />
<SearchInput placeholder="Buscar por folio o cliente" style={{ width: 320 }} />
```

Pill + fullWidth in the topbar; plain radius and a fixed width in table toolbars. Focus tints the hairline orange.
