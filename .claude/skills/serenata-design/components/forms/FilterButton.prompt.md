The "Filtrar" control that sits next to a contextual search field above a table.

```jsx
<FilterButton count={2} onClick={openFilters} />
```

Always a secondary button with a trailing chevron — never orange.
