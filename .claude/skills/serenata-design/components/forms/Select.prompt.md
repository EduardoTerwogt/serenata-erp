Small dropdown used for "Resultados por página" and other inline choices.

```jsx
<Select options={[10, 25, 50]} defaultValue={10} />
```

Keep it visually quieter than a secondary button; it is a setting, not an action.
