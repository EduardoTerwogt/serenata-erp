Brand identity placeholder — renders in display type because no logo files exist in this system yet.

```jsx
<Wordmark variant="mark" size={26} />      // sidebar: square "S" only
<Wordmark variant="wordmark" tone="white" />
```

The app sidebar uses `variant="mark"`. Replace with the supplied SVG as soon as it is available; never redraw the real isotipo by hand.
