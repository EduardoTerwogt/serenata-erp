Avance de un proyecto o cobertura de un presupuesto.

```jsx
<ProgressBar value={62} />
<ProgressBar value={84} height={7} tone="var(--sn-status-approved-bg)" />
```

- Pon el porcentaje como texto aparte si hace falta; el componente no lo dibuja.
